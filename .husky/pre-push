sh scripts/git-identity-guard.sh outgoing

# Load-bearing, not belt-and-braces: `next lint` was removed in Next 16 and
# `next build` no longer lints. Without this, lint errors reach main silently.
pnpm lint
